# UnShrimp Posture NN

This folder is reserved for the trained neural-network classifier and its preprocessing metadata.

Training command:

```powershell
python ml\scripts\train_nn.py --data-dir data\gmo --model-dir models\unshrimp_posture_nn --eval-dir ml\Eval --epochs 100 --batch-size 32
```
